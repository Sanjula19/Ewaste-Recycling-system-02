# Component 1 frontend — configuration
# All calls go through the API Gateway, never directly to the backend port,
# so this is the only URL this app needs to know about.
VITE_GATEWAY_URL=http://localhost:8080
